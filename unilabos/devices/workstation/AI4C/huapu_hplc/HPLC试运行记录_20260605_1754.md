# 华谱 HPLC 试运行记录

## 基本信息

- 试运行时间：2026-06-05 17:54
- 驱动：`unilabos/devices/hplc/huapu_hplc.py`
- 华谱 HTTP 地址：`http://10.10.10.1:8001`
- 报告导出目录：`E:\dev\works\Uni-Lab-OS-xyf1\unilabos\devices\workstation\AI4C\huapu_hplc`
- 本次 `procInstId`：`605175414`
- 结果：序列创建成功，运行序列失败，未生成报告文件。

## 1. 前准备

### 1.1 工作站、项目、方法

当前华谱接口文档中没有提供“新建工作站 / 新建项目 / 新建方法”的 HTTP 接口，因此本次试运行使用已有项目和方法。

获取到的项目列表：

```json
[
  {
    "projectId": 1,
    "projectName": "default-project"
  },
  {
    "projectId": 2,
    "projectName": "test"
  }
]
```

本次选择：

- 项目：`projectId=1`，`projectName=default-project`
- 仪器：`instrumentId=1`，`instrumentName=HPLC`

### 1.2 建立连接、仪器整备

调用 `/project/findInstrumentList` 成功，仪器在线：

```json
[
  {
    "id": 1,
    "instrumentName": "HPLC",
    "onlineStatus": 1
  }
]
```

调用 `/project/getDeviceStatus` 后，服务端返回仪器侧异常：

```json
{
  "msg": "连接异常",
  "code": 3321
}
```

调用 `/project/getErrorDetail` 返回同样的错误：

```json
{
  "msg": "连接异常",
  "code": 3321
}
```

结论：HTTP 服务可访问，仪器列表显示在线，但仪器状态接口返回 `连接异常`。本次运行失败点在仪器侧连接或整备状态，不是 Uni-Lab 驱动无法访问 HTTP 服务。

## 2. 序列编辑

### 2.1 获取项目

接口：`POST /project/findAllProjectList`

结果：成功，见上方项目列表。

### 2.2 根据项目 ID 获取方法

接口：

- `POST /project/findSequenceMethodList`
- `POST /project/findProcessMethodList`
- `POST /project/findReportMethodList`

项目 `projectId=1` 下获取到：

序列方法：

```json
[
  {
    "id": 2,
    "methodName": "List_1"
  }
]
```

处理方法：

```json
[
  {
    "id": 3,
    "methodName": "Processing Method_new"
  },
  {
    "id": 2,
    "methodName": "Processing Method_1"
  }
]
```

报告方法：

```json
[
  {
    "id": 5,
    "methodName": "样品综合报告"
  },
  {
    "id": 11,
    "methodName": "结果报告"
  }
]
```

本次选择：

- `sequenceMethodId=2`
- `proMethodId=3`
- `reportMethodId=5`

## 3. 发送序列方法 ID，运行序列

### 3.1 新建序列方法

接口：`POST /project/addSequenceMethod`

请求要点：

```json
{
  "procInstId": 605175414,
  "projectId": 1,
  "sequenceMethodId": 2,
  "name": "unilab_trial_605175414",
  "seqTrayNum": 0,
  "sampleArr": [
    {
      "trayNum": 0,
      "xPosition": "A",
      "yPosition": 1,
      "sampleName": "unilab_trial_605175414"
    }
  ],
  "instIdArr": [1]
}
```

响应：

```json
{
  "code": "0",
  "errCode": null,
  "msg": "success",
  "data": null
}
```

结论：序列创建成功。

### 3.2 运行序列

接口：`POST /project/runSequence`

请求要点：

```json
{
  "procInstId": 605175414,
  "proMethodId": 3,
  "reportMethodId": 5,
  "shutDown": 0,
  "exportPath": "E:\\dev\\works\\Uni-Lab-OS-xyf1\\unilabos\\devices\\workstation\\AI4C\\huapu_hplc"
}
```

响应：

```json
{
  "code": "1",
  "errCode": "1",
  "msg": "连接异常",
  "data": null
}
```

结论：运行序列失败，服务端返回 `连接异常`，序列没有进入运行状态。

## 4. 暂停信号

本次序列未成功启动，因此没有发送暂停信号。

暂停接口应为：`POST /project/controlSequence`

请求示例：

```json
{
  "action": 1,
  "procInstId": 605175414
}
```

## 5. 序列完成或错误终止

运行失败后查询序列状态。

接口：`POST /project/getSequenceStatus`

响应：

```json
{
  "code": "0",
  "errCode": null,
  "msg": "success",
  "data": {
    "seqTrayNum": 0,
    "status": 0
  }
}
```

结论：序列已创建但未开始运行，状态为 `0`。当前文档中只定义了 `1-进行中`、`2-已完成`、`3-暂停`、`4-终止`、`5-异常终止`，`0` 在本次表现为未运行/未知状态。

## 6. 获取结果、报告

由于 `/project/runSequence` 未成功，未生成结果和报告文件。

检查目录：

`E:\dev\works\Uni-Lab-OS-xyf1\unilabos\devices\workstation\AI4C\huapu_hplc`

当前未发现本次试运行生成的报告文件。

## 后续处理建议

1. 在华谱软件或仪器服务端确认 HPLC 仪器系统已真正连接并完成整备。
2. 再次调用 `/project/getDeviceStatus`，确认返回 `deviceStatus`，而不是：

```json
{
  "msg": "连接异常",
  "code": 3321
}
```

3. 仪器连接恢复后，可优先复用本次已创建的 `procInstId=605175414` 重新调用 `/project/runSequence`。
4. 如果服务端要求每次重建序列，则需要换一个新的 int 类型 `procInstId`，避免同一项目内重复。
5. 若仍需把报告导出到当前仓库目录，需要确认华谱 HTTP 服务进程所在机器能访问该路径；否则应先导出到服务端可访问目录，再人工或脚本拷贝到本目录。
